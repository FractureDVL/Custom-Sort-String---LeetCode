/**
 * @param {string} order
 * @param {string} s
 * @return {string}
 */
var customSortString = function (order, s) {
    
    var o = s.split('');
    var res = s.split('');

    for (let i = 0; i < order.length; i++) {
        for (let j = 0; j < s.length; j++) {
        }
    }
    
    return console.log(res)
};

customSortString("cbad","abcd");